# tcp-p2p

Peer-to-peer bootstrap server and TCP chat client with NAT traversal, built on gVisor's userspace TCP stack. Encapsulates TCP packets inside of UDP packets.
Bootstrap server made with custom UDP text-based protocol.